key: m5stack-mcu-u035
name: Unit CardKB controller
manufacturer: M5Stack
tags: [keyboard, input]
addresses:
  - addr: "0x5f"
datasheets:
  - source: vendor
    vendor: m5stack
    url: https://docs.m5stack.com/en/unit/cardkb
notes: |
  Firmware-defined I2C slave: the I2C endpoint is an on-board ATmega8A running M5Stack
  firmware (not a fixed-function IC), so the register map is product-specific and is grown
  from captures rather than a datasheet. Modeled per-unit. Silicon: ATmega8A. Used in M5Stack Unit CardKB (U035) at 0x5f.
