key: m5stack-mcu-u139
name: Unit ACSSR controller
manufacturer: M5Stack
tags: [relay, ssr, ac]
addresses:
  - addr: "0x50"
datasheets:
  - source: vendor
    vendor: m5stack
    url: https://docs.m5stack.com/en/unit/ACSSR
notes: |
  Firmware-defined I2C slave: the I2C endpoint is an on-board ESP32-C3 running M5Stack
  firmware (not a fixed-function IC), so the register map is product-specific and is grown
  from captures rather than a datasheet. Modeled per-unit. Silicon: ESP32-C3. Used in M5Stack Unit ACSSR (U139) at 0x50.
